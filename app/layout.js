import "./globals.css"

export const metadata = {
  title: "Generador de Codigos de Barra",
};

export default function GeneradorCodigosDeBarraLayout({ children }) {
  return (
    <html>
      <body>
        {children}
      </body>
    </html>
  );
};